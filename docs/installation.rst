============
Installation
============

At the command line::

    $ easy_install django-soft-delete-model-mixin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-soft-delete-model-mixin
    $ pip install django-soft-delete-model-mixin
